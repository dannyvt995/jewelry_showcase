"use client"
import LoadEviroment from '../LoadEnviroment'
import LoadControls from '../LoadControls'
import { Environment } from '@react-three/drei'

export default function SceneWrapper({ children }) {
    return (
        {children}
    )
  }